name: elasticsearch_curator publish
on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths:
      - 'elasticsearch-curator/**'

jobs:
  Publish-elasticsearch_curator-Package:
    runs-on: windows-latest
    steps:
      - run: echo 'The triggering workflow passed'
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: 📦 Pack Chocolatey Package
        run: choco pack elasticsearch-curator/elasticsearch-curator.nuspec
      - name: 🔐 Set Chocolatey API-KEY
        run: choco apikey --api-key ${{ secrets.API_KEY }} -source https://push.chocolatey.org/
      - name: 🚀 Push Chocolatey Package
        run: choco push -source https://push.chocolatey.org/
